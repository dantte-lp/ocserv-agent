# ocserv-agent Environment Variables
# Copy this file to .env and fill in your actual values

# Remote test server configuration (RFC 5737 example)
REMOTE_HOST=192.0.2.1
REMOTE_USER=root
REMOTE_PASSWORD=your_secure_password_here

# Alternative: use SSH key instead of password (recommended)
# Leave REMOTE_PASSWORD empty and configure ~/.ssh/config
